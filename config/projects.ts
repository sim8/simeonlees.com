import { Project } from '../types';

const projects: Project[] = [
  {
    title: 'Learn Regex',
    slug: 'learn-regex',
    descriptionHtml: '<p>Really nice</p>',
    previewUrl: '',
  },
];

export default projects;
